@model TouristWebSite.Models.ActiveNewsViewModel

<h2>Найсвіжіші новини</h2>
<p class="text-success">@ViewBag.StatusMessage</p>
<h4>Переглядайте наші новини.</h4>

@foreach (var item in Model.ActiveNews)
{
    <div class="jumbotron transparent transp-container">
        @if (User.IsInRole("Admin"))
        {
            <a href="@Url.Action("DeleteNew", "News", new {itemId = item.Id})" title="Видалити">
                <i class="fas fa-times fa-lg" style="float:right;"></i>
            </a>
            <a href="@Url.Action("EditNew", "News", new {itemId = item.Id})" title="Редагувати">
                <i class="fas fa-edit fa-lg" style="float:right; margin-right: 5px"></i>
            </a>
        }
        <h3>@item.Name</h3>
        <h4>@item.Content</h4>
        @if (item.Link != null && item.Link != string.Empty)
        {
            <h4><a href=@item.Link>Детальніше за посиланням...</a></h4>
        }
        <label style="float: right"><i>@item.Date.ToShortDateString()</i></label>
    </div>
}

@if (User.IsInRole("Admin"))
{
    <a class="btn btn-default no-underline" href="@Url.Action("AddNew", "News")"> <span> <i class="fas fa-plus"></i> Додати нову новину </span> </a>
}

<h2>Наші акції</h2>
<p class="text-success">@ViewBag.StatusMessage1</p>
<h4>Переглядайте наші акції.</h4>

@foreach (var item in Model.ActiveDiscounts)
{
    <div class="jumbotron transparent transp-container">
        @if (User.IsInRole("Admin"))
        {
            <a href="@Url.Action("EditDiscount", "News", new {itemId = item.Id})" title="Редагувати">
                <i class="fas fa-edit fa-lg" style="float:right;"></i>
            </a>
        }

        <h3>@item.Name</h3>
        <h4>@item.Content</h4>
        @if (item.Link != null && item.Link != string.Empty)
        {
            <h4><a href=@item.Link>Детальніше за посиланням...</a></h4>
        }
        <label style="float: right"><i>@item.StartDate.ToShortDateString() - @item.EndDate.ToShortDateString() </i></label>
    </div>
}

@if (User.IsInRole("Admin"))
{
    <a class="btn btn-default no-underline" href="@Url.Action("AddDiscount", "News")"> <span> <i class="fas fa-plus"></i> Додати нову акцію </span> </a>
}